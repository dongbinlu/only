<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.only.multids.busi.dao.OrderMapper">


    <insert id="insertOrder" parameterType="com.only.multids.busi.bean.Order">
        insert into order${tableSuffix}(order_id, user_id, money)
        values (#{orderId}, #{userId}, #{money})
    </insert>

    <select id="getByOrderId" resultType="com.only.multids.busi.bean.Order">
        select order_id as orderId,
               user_id  as userId,
               money    as money
        from order${tableSuffix}
        WHERE order_id = #{orderId}
    </select>
</mapper>